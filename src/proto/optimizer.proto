syntax = "proto3";
package optimizer;

service Optimization {
    rpc serve_optimization(OptimizationRequest) returns (stream OptimizationResponse);
}

message OptimizationRequest {
    repeated string model_names = 1 ; // List of model to ask optimization for
}

message OptimizationResponse {
}